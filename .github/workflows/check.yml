on: 
  workflow_dispatch:    
  schedule:
    - cron: 0 0,12 * * *

jobs:
  check-usage:
    name: "Healthcheck"
    runs-on: ubuntu-latest
    steps:
      - name: "Checkout"
        uses: actions/checkout@v4
        
      - name: "Install bun"
        uses: oven-sh/setup-bun@v1
      
      - name: "Check"
        run: |
          bun install && bun test
        


  
